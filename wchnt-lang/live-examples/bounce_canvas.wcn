# Bounce on canvas (live interpreter)

Same Schema, Construction, and Methods as `bounce_openfl.wcn`. Target is `%canvas`: step the game, then `draw`. The Haxe backend does not emit this host; use `compile-to-ir` and the interpreter (`doc/live.md`).

## Schema

```
Game = PlayArea Ball
PlayArea = Rect
Rect = Int/x Int/y Int/width Int/height
Ball = Int/x Int/y Int/dx Int/dy Int/rad
```

## Construction

```
[:Game
  [:PlayArea [0 0 800 600]]
  [:Ball 200 150 6 5 16]
]
```

## Methods

```
Game::bounceDx = {
  r = playArea.rect.
  if ((ball.x < r.x) or (ball.x > (r.x + r.width))) { -ball.dx } else { ball.dx }
}

Game::bounceDy = {
  r = playArea.rect.
  if ((ball.y < r.y) or (ball.y > (r.y + r.height))) { -ball.dy } else { ball.dy }
}

Game::step = {
  ndx = this.bounceDx().
  ndy = this.bounceDy().
  [:Game playArea [:Ball (ball.x + ndx) (ball.y + ndy) ndx ndy ball.rad]]
}

Ball::draw = { @WCHNTGraphics/g |
  g.beginFill(15921906).drawCircle(x, y, rad).endFill()
}

Game::draw = { @WCHNTGraphics/g |
  r = playArea.rect.
  bg = g.beginFill(2769450).drawRect(r.x, r.y, r.width, r.height).endFill().
  ball.draw(g)
} -> Void
```


## Target

```
%canvas

%init
var assemblage;

function init() {
    assemblage = GameAssemblage.factory();
}

%step
function step() {
    assemblage = assemblage.step();
    wchntGraphics.clear();
    assemblage.draw(wchntGraphics);
}
```
