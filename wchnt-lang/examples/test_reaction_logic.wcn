# Example: comparison and boolean methods

## Schema

```
Game = Rect Ball
Rect = Int/x Int/y Int/width Int/height
Ball = Int/x Int/y Int/dx Int/dy Int/rad
```

## Construction

```
[:Game
  [0 0 800 600]
  [100 100 1 1 5]
]
```

## Methods

```
Ball::movingRight = { dx > 0 }

Rect::contains = {px, py | (px >= x) and (px <= (x + width)) and (py >= y) and (py <= (y + height))}
```

## Target

```
%terminal

%main
public static function main():Void {
    var assemblage = GameAssemblage.factory();
    var helper = new WCHNTHelper();
    trace(assemblage.toConstruction(0, helper));
}
```
