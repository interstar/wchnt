# Methods

**This document moved to [`method.md`](method.md).**

`reaction.md` is kept as a redirect for old links. The canonical Methods spec — including `update`, identity slots, and in-place mutation for `$` / `>` objects — is in **`method.md`**.

`reaction_phase.md` is an archived early sketch; see the note at the bottom of `method.md`.
